def difference(s1, s2):
    diff = 0
    for a, b in s1, s2:
        if a != b:
            diff += 1
    return diff

try:
    s1 = input()
    s2 = input()
    distance = 0
    maker = 0
    while len(s1) > len(s2):
        best_match
        for x in range(len(s2) - len(s1)):

        maker += 1
    while len(s1) < len(s2):

    while s1 != s2:

except EOFError:
    exit()
